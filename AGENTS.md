# 教学能力比赛专家系统 — Agent使用说明

## 可用Agent

### jingsai-coach（主Agent）
备赛实战教练，负责生成建议、检索知识库、派发审校、执行文档修改。
- 思维模式：进取型——"怎么做到最好"
- 权限：读写文件、执行bash、联网搜索、派发子任务

### jingsai-reviewer（审校Agent）
政策标准审查员，核查建议的合规性和准确性。
- 思维模式：防御型——"这样做会不会违规/出错"
- 权限：只读+搜索（不修改文件）

### jingsai-judge（评审Agent）
模拟评委视角，找逻辑漏洞和扣分风险。
- 思维模式：挑刺型——"如果我是评委会从哪里扣分"
- 权限：只读+搜索（不修改文件）

### jingsai-formatter（版式审查Agent）
版式排版审查员，专门检查字体、字号、配色、留白、图文排布等版式问题。
- 思维模式：版面感优先型——"评委第一眼看到什么"
- 权限：只读+搜索（不修改文件）
- 注意：字体/字号建议仅供参考（不同专家意见不一），配色方案需人工确认

## 使用方式

主Agent（jingsai-coach）通过 task 工具派发审校任务。审校Agent不需要直接调用。

如需直接使用某个审校Agent：
```
@jingsai-reviewer 请审查这份实施报告的政策合规性
@jingsai-judge 请从评委角度评估这份教案
@jingsai-formatter 请检查这份文档的版式排版问题
```

## 审校流程

1. Coach生成初版建议
2. Coach并行派发给Reviewer、Judge和Formatter
3. 收集反馈：
   - 三者都认可 → 直接输出
   - 任一提出异议 → 标注【争议点】
   - 两者以上反对 → 重新生成

## 工具脚本

详见 `.opencode/skills/jingsai-tools/SKILL.md`
